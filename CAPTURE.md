# Windows 수집·작업 경계·모델 호출 설계

이 문서는 Here가 Windows에서 무엇을 관측하고, 어디서 작업 맥락을
끊으며, 어떤 근거만 모델에 보내는지에 대한 현재 구현 계약입니다.

## 1. 수집 루프

Windows 빌드는 `get-windows` 9.3.0의 네이티브 구현을 사용합니다. 내부적으로
`GetForegroundWindow`, `GetWindowTextW`, 프로세스 ID/실행 경로와 창 경계를
읽습니다. 관리자 권한이나 키보드·마우스 hook은 사용하지 않습니다.

| 항목 | 현재 계약 |
| --- | --- |
| 주기 | 500ms foreground snapshot |
| 읽기 제한 | 1.5초; 중복 poll은 동시에 실행하지 않음 |
| 동일 창 | 이벤트를 추가하지 않고 `lastSeenAt`, `sampleCount`, 경계만 갱신 |
| 제목 변경 | HWND가 같아도 별도 이벤트로 기록 |
| 보호된 창 | 앱·제목·PID 없이 `capture-gap: protected` 하나만 기록 |
| 읽기 실패 | 3회 연속 실패 시 내용 없는 `capture-gap: unavailable` 기록 |
| 보관 | 기본 10분, 최대 1,500개, 프로세스 메모리만 사용 |
| 상태 | 시도/성공/실패/연속 실패/마지막 성공/health를 메모리에서 계측 |

따라서 브라우저 탭이나 문서 제목이 500ms 이상 유지되면 같은 HWND에서도
전환으로 남습니다. 반대로 같은 창 안의 버튼 클릭처럼 앱·창 제목이 전혀
바뀌지 않는 행위는 의도적으로 수집하지 않습니다. 500ms보다 짧게 나타났다
사라지는 창은 polling 방식의 한계상 누락될 수 있습니다.

## 2. 화면 이미지

화면 이미지는 기본 비활성화이며 자동 수집되지 않습니다. 사용자가 옵션을
켠 상태에서 복원 또는 `나중에 이어보기`를 직접 실행했을 때만 다음 검사를
모두 통과하면 한 장을 만듭니다.

1. 복원 직전에 foreground snapshot을 새로 읽습니다.
   이 읽기가 실패하면 직전 창을 현재 창으로 재사용하지 않습니다.
2. 원래 이벤트에 유효한 활성 창 경계가 있어야 합니다. 경계가 없으면 전체
   화면으로 대체하지 않습니다.
3. Here 창을 잠깐 숨긴 뒤 다시 snapshot하여 같은 이벤트 ID인지 확인합니다.
4. 실제 thumbnail 크기와 Electron DIP 좌표의 X/Y 배율을 각각 계산해 해당
   창과 디스플레이가 겹치는 영역만 crop합니다.
5. 최대 폭 1,280px, JPEG 72 품질, 1.5MB 이하만 사용합니다.
6. 이미지에는 `sourceEventId`와 `capturedAt`을 붙여 근거 창과 연결합니다.

이미지는 타임라인에 자동 저장되지 않습니다. 명시적으로 저장한 체크포인트만
OS `safeStorage`로 전체 암호화되어 최대 12개·7일 보관됩니다.

## 3. 작업 맥락 경계

LLM은 작업 경계를 결정하지 않습니다. 모델 호출 전에 아래 결정적 규칙으로
최대 5단계의 체인을 확정합니다.

1. **사람의 판단:** pause/resume 뒤에는 이전 기록을 넘지 않는 hard boundary.
2. **정확한 복귀:** 앱과 정규화한 창 제목이 같은 `A → non-A/gap → A`는
   return chain. 브라우저처럼 HWND가 같아도 제목이 다르면 다른 대상으로 봅니다.
3. **비활동:** 정확한 복귀가 아니며 관측 사이가 4분 이상 비면 새 작업 구간.
4. **최근 흐름:** 위 조건이 없으면 현재 구간의 최신 관측만 사용.
5. **명시적 장기 맥락:** 사용자가 `나중에 이어보기`를 누른 체크포인트는 별도
   암호화된 사람 주도 경계이며, 자동 타임라인과 섞지 않습니다.

복귀 사이에 보호된 창이 있었다면 “내용 기록 안 함”만 표시하고 그 앱의
정체는 UI나 모델에 전달하지 않습니다. 관측된 다른 창이 있는 exact return은
high, 보호 구간만 있는 return이나 두 창 연결은 medium, 현재 창 하나뿐이면
low confidence입니다.

## 4. 모델로 보내는 데이터와 호출 예산

모델은 선택 사항입니다. UI는 항상 로컬 체인을 먼저 표시하고 다음 조건을
모두 만족할 때만 비동기 보강을 시작합니다.

- confidence가 medium 또는 high
- 모델에 보낼 수 있는 실제 foreground evidence가 2개 이상
- 저장된 체크포인트에 이미 model 결과가 있지 않음

모델 입력에는 선택된 최대 5개 이벤트의 `id`, ISO 시각, 앱 이름, 창 제목,
종류와 현재 대상만 들어갑니다. HWND, PID, 실행 경로, bounds, 보호 구간,
전체 10분 타임라인은 보내지 않습니다. 선택적 이미지가 있으면 한 장만
추가합니다.

| 제어 | 기본값 |
| --- | --- |
| OpenAI-compatible 출력 상한 | 320 tokens |
| 동일 근거 결과 캐시 | 2분, 최대 64개 |
| 동일 동시 요청 | 하나의 in-flight 요청으로 합침 |
| runtime HTTP 요청 | 분당 최대 4회 |
| 보수적 추정 사용량 | 분당 최대 12,000 tokens |
| 응답 제한 | 30초, 1MB |

JSON Schema → JSON object → prompt-only 호환성 재시도도 위 요청/TPM 예산을
각각 소비합니다. 예산을 넘거나 endpoint가 실패하면 기다리거나 재시도하지
않고 즉시 기존 로컬 결과를 유지합니다. 연결 테스트는 사용자가 명시적으로
누르는 진단이므로 runtime 예산과 분리됩니다.

## 5. 자동 검증과 남은 실기기 gate

단위·시뮬레이션 테스트는 다음을 검증합니다.

- Windows HWND 재사용 상태의 제목/페이지 변경
- 보호 앱 전환 뒤 같은 문서 복귀
- 10분보다 오래 열린 현재 창 유지와 오래된 이벤트 정리
- 네이티브 reader 반복 실패·복구·health·1,500개 상한
- pause/resume, 4분 비활동, exact return 경계
- DPI/음수 모니터 좌표/부분 화면의 crop 계산
- LLM 캐시, 동시 호출 합치기, 요청/추정 TPM 초과 시 로컬 fallback

GitHub Actions의 Windows runner는 테스트 뒤 Electron ABI로 실제
`get-windows.activeWindow()`를 한 번 호출해 네이티브 모듈 load와 응답 제한을
smoke-test합니다. 다만 hosted runner는 실제 사무용 데스크톱이 아니므로 아래는
릴리스 후보를 일반 Windows PC에서 확인해야 하는 최종 gate입니다.

1. Excel A → 브라우저 B → Excel A에서 세 전환이 순서대로 보이는지
2. 같은 Chrome 창에서 서로 다른 탭 제목이 각각 남는지
3. 300ms 미만 전환이 누락될 수 있음을 제품 한계로 허용할지
4. 125%/150% DPI와 다중 모니터에서 이미지가 해당 창만 자르는지
5. 관리자 권한 앱/UAC/잠금 화면에서 generic gap으로 안전하게 내려가는지

원시 click·key·문서 본문을 추가하면 1번 신호는 더 풍부해지지만 제품의
프라이버시 성격이 바뀝니다. 현재 단계에서는 foreground/title만 유지하고,
500ms 미만 전환까지 반드시 잡아야 한다는 실기기 결과가 나올 때만
`SetWinEventHook(EVENT_SYSTEM_FOREGROUND/EVENT_OBJECT_NAMECHANGE)` 기반 네이티브
이벤트 수집기를 다음 단계로 올리는 것이 권장 경로입니다.
